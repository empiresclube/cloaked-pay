# Solana network for the wallet adapter
# Options: devnet | testnet | mainnet-beta
VITE_SOLANA_NETWORK=devnet

# Optional custom RPC endpoint. If empty, the default cluster API is used.
# Recommended for production: a Helius/QuickNode/Triton RPC URL.
VITE_SOLANA_RPC_URL=
